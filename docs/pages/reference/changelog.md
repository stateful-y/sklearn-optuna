---
description: Release history for every published version, with breaking changes, new features, and fixes.
---

# Changelog

<!--
The H1 above belongs to this page, not to CHANGELOG.md.  The include starts at
line 3 to skip CHANGELOG.md's own "# Changelog" heading -- without that offset
the page renders two identical H1s.  The heading has to live here rather than
arrive with the include: the SUBPAGES list on index.md reads this file's source
to find a title, and snippets are not expanded at that point, so a page whose
only H1 comes in via --8<-- looks untitled, drops out of the index, and fails
the --strict build.
-->

--8<-- "CHANGELOG.md:3"
